##(:*(Type)T :: T) (Nat).
##((:(Nat)n) :=> n) (42).

##((:*(Type)T :(T)x :: T) :=> x) (Nat . 42).

##2 // 3.
##1, 2

##let: #x with: 42.
##x

#*
## returnInt :: () => Int32.
procedure: #returnInt with: {
    ReturnInteger(42)
}.

## Set the module entry point.
entryPoint: returnInt.
*#
